<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.utc.applogeo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Applogeo">
        <activity android:name=".AgregarClienteFactura"></activity>
        <activity android:name=".FacturacionActivity" />
        <activity android:name=".RegistrarNuevaVentaActivity" />
        <activity android:name=".EditarClienteActivity" />
        <activity android:name=".VentasActivity" />
        <activity android:name=".ProductosActivity" />
        <activity android:name=".ClientesActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".Bienvenida">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroActvty" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>